name: flutter_icp_auth
description: "A Flutter-ICP package to automate internet identity authentication."
version: 2.0.0
homepage: https://github.com/ICP-hub/flutter_icp_auth

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  agent_dart: ^1.0.0-dev.22
  flutter_custom_tabs: ^2.0.0+1
  flutter_secure_storage: ^9.2.1
  shared_preferences: ^2.2.3
  uni_links: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
