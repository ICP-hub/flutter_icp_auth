import 'dart:developer';

class DelegationValidation {
  static delegationValidation(pubKey, priKey, delegation) {
    try {
      // TODO
    } catch (e) {
      log("Error: $e");
      throw 'Invalid delegation data';
    }
  }
}
